{
    "type": "object",
    "title": "response",
    "properties": {
        "body": {
            "type": "string",
            "description": "the message the model wishes to send"
        }
    },
    "required": ["body"]
}